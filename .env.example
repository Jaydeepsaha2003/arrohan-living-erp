# Copy this file to .env and fill in your own values.
# .env is git-ignored — it never gets committed or pushed.
#
# Leave both blank (or delete this file) to use a local SQLite file in data/
# instead — that's the default for the office install and needs no setup.

TURSO_DATABASE_URL=
TURSO_AUTH_TOKEN=
