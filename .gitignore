node_modules/

# Your database and the first-run passwords — never commit these.
data/

# Your own company details (GSTIN, PAN, bank). Commit only the .example file.
config/company.json

# Built output, regenerated by `npm run build`.
server/public/

*.log
backup-*.db
.env
.DS_Store
Thumbs.db
